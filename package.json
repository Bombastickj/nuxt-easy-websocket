{
  "name": "nuxt-easy-websocket",
  "version": "4.1.2",
  "description": "Easy WebSocket integration module for nuxt",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bombastickj/nuxt-easy-websocket.git"
  },
  "license": "MIT",
  "author": "Jonas Brussat, Alexander Führer",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "typesVersions": {
    "*": {
      ".": [
        "./dist/types.d.mts"
      ]
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "nuxt-module-build build",
    "dev": "nuxi dev playground",
    "dev:build": "nuxi build playground",
    "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
    "release": "npm run lint && npm run test && npm run prepack && changelogen --release && npm publish && git push --follow-tags",
    "release:alpha": "npm run lint && npm run test && npm run prepack && changelogen --release --versionSuffix alpha.5 && npm publish && git push --follow-tags",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
  },
  "dependencies": {
    "crossws": "^0.4.1",
    "defu": "^6.1.4",
    "knitwork": "^1.1.0",
    "oxc-parser": "^0.130.0",
    "pathe": "^2.0.1",
    "scule": "^1.3.0"
  },
  "devDependencies": {
    "@nuxt/devtools": "^3.2.4",
    "@nuxt/eslint-config": "^1.2.0",
    "@nuxt/kit": "^4.4.5",
    "@nuxt/module-builder": "^1.0.1",
    "@nuxt/schema": "^4.4.5",
    "@nuxt/test-utils": "^4.0.3",
    "@types/node": "latest",
    "changelogen": "^0.6.1",
    "eslint": "^10.3.0",
    "nuxt": "^4.4.5",
    "playwright-core": "^1.59.1",
    "prettier": "^3.6.2",
    "typescript": "latest",
    "vitest": "^4.1.6",
    "vue-tsc": "^3.0.1",
    "zod": "^4.4.3"
  },
  "overrides": {
    "crossws": "^0.4.1"
  }
}
